spring:
  config:
    # Load secrets from a gitignored .env file (KEY=VALUE, properties format).
    # optional: means the app still starts if the file is absent (e.g. in CI/prod
    # where real environment variables are provided instead).
    import: "optional:file:./.env[.properties]"
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/interviewprep}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
  jwt:
    secret: ${JWT_SECRET:myDefaultSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm}
    expiration: ${JWT_EXPIRATION:86400000}
    refresh-expiration: ${REFRESH_TOKEN_EXPIRATION:604800000}
  groq:
    api-key: ${GROQ_API_KEY:}
    base-url: ${GROQ_BASE_URL:https://api.groq.com/openai/v1}
    model: ${GROQ_MODEL:llama-3.3-70b-versatile}
    whisper-model: ${GROQ_WHISPER_MODEL:whisper-large-v3-turbo}
  gemini:
    api-key: ${GEMINI_API_KEY:}
  resume:
    upload-dir: ${RESUME_UPLOAD_DIR:./uploads/resumes}
